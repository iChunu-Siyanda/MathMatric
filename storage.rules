rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {

    // Allows authenticated users to read all files in exam-papers (including nested folders)
    match /exam-papers/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if false;
    }

  }
}
