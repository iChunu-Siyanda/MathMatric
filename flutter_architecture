Widget Tree in the Application:
lib
 |
 |--app
 |   |-app.dart
 |   |-bloc_observer.dart
 |   |-router.dart
 |   |-factories
 | 
 |
 |--features
     |- Auth
     |- Drawer
     |- Home
     |- Papers
     |- Streak
          

Flow of Data in the Application:

UI (Widgets)
│
├── PaperTile → uses PaperItem
│
├── TopicsSliver → uses PaperItem.section.topics
│
├── ExamViewer → uses ExamPaper
│
└── Repository
    ├── getPaperStructure() → PaperItem
    └── getExamPapers() → List<ExamPaper>

